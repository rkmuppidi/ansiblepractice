---
 - hosts: all
   become: true
   tasks:
     - name: "installing the httpd pck"
       yum: name=httpd state=present
     - name: "copy the source file"
       copy: src=/srv/file.txt  dest=/home/shashd1
